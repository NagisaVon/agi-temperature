// Route B won the bake-off on user preference (see ../DECISION.md) and
// graduated to ../ProductionScene.tsx; this re-export keeps the /spike
// harness comparing it against the parked Route A prototype.
export { default } from '../ProductionScene';

// Route A won the bake-off (see ../DECISION.md) and graduated to
// ../ProductionScene.tsx; this re-export keeps the /spike harness comparing
// the production scene against the parked Route B prototype.
export { default } from '../ProductionScene';
